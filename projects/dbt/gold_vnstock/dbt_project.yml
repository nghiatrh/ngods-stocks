name: 'gold_vnstock'
version: '1.0.0'
config-version: 2

profile: 'spark'

model-paths: ["models"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gold_vnstock:
    +materialized: table
    +schema: gold
